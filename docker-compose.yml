services:
  jekyll:
    build: .
    ports:
      - "8080:8080"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll:cached  # mount source files, keep gems in container
    environment:
      - JEKYLL_ENV=development
